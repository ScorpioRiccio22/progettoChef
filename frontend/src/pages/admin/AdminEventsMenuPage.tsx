import AdminMenuPage from '@/pages/admin/AdminMenuPage'

/** Stessa logica del menu del negozio (piatti + prezzo + un solo attivo), applicata agli eventi. */
export default function AdminEventsMenuPage() {
  return <AdminMenuPage type="EVENTS" />
}
